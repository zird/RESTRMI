<project name="ServerCarRenting" default="jar" basedir=".">
	<description>
		Server for car renting service
	</description>
	<property name ="srcdir" location="src/web/" />
	<property name ="builddir" location="build/" />
	<property name ="targetdir" location="./" />
	<property name ="mainclass" value="web.classes.CarServer" />

	<target name="init">
		<tstamp/>
		<mkdir dir="${builddir}" />
	</target>

	<target name="compile" depends="init" description="compile the source">
		<javac srcdir="${srcdir}" destdir="${builddir}" includeantruntime="false" />
	</target>

	<target name="jar" depends="compile" description="generate the jar">
		<jar jarfile="${targetdir}/${ant.project.name}.jar" basedir="${builddir}">
			<fileset file="grant.policy" />
			<manifest>
				<attribute name="Main-Class" value="${mainclass}"/>
			</manifest>
		</jar>
	</target>

	<target name="clean">
		<delete dir="build" />
		<delete file="${targetdir}/${ant.project.name}.jar" />
	</target>

</project>